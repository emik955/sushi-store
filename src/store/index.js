import { createStore } from 'vuex'

import cartProducts from './modules/cartProducts'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    cartProducts
  }
})
